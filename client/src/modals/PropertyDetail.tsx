import ModalBase from "@/components/BaseModal";
import React from "react";

const PropertyDetail = ({ children }) => {
  return <ModalBase triggerComponent={children}>ansh</ModalBase>;
};

export default PropertyDetail;
